namespace BU.Enums
{
    /// <summary>
    /// Represent the state of a publication (lisibility).
    /// </summary>
    public enum PublicationState
    {
        Unknown = 0,
        Unreadable = -1,
        Readable = 1
    }
}
