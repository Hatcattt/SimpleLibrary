<UserControl x:Class="WpfApp.View.Publication.PublicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Margin="20" Header="Publication view" FontSize="13">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" Grid.Column="0">
                <StackPanel Margin="10">

                    <Border 
                        CornerRadius="15"
                        Background="#FFE2E2E2">

                        <StackPanel
                            x:Name="StackPanelSearch"
                            Orientation="Horizontal"
                            Margin="5">
                            <Image 
                                Width="40"
                                Margin="5"
                                Source="/image/icon/128/publication_search.png"/>
                            <Label 
                                VerticalAlignment="Center"
                                Margin="0,3,3,3"
                                FontSize="14"
                                FontWeight="DemiBold"
                                Content="Search By">
                            </Label>

                            <ComboBox 
                                ToolTip="Select one..."
                                Height="30"
                                Width="93"
                                x:Name="SearchWord"
                                Margin="0,0,5,0">
                            </ComboBox>

                            <TextBox
                                FontSize="13"
                                Margin="0,0,5,0"
                                Padding="5"
                                VerticalContentAlignment="Center"
                                HorizontalAlignment="Left" 
                                x:Name="SearchInput" 
                                Width="150"
                                Height="30"/>

                            <Button 
                                ToolTip="Reset"
                                Margin="0,0,5,0"
                                FontSize="20"
                                Height="30"
                                Width="30">
                                <Image 
                                    Width="20"
                                    Source="/image/icon/32/reset_icon.png"/>
                            </Button>
                        </StackPanel>
                    </Border>
                    <DataGrid
                        IsReadOnly="True"
                        BorderThickness="3"
                        Margin="0,10,0,0"
                        ItemsSource="{Binding Publications}"
                        AutoGenerateColumns="False"
                        x:Name="PublicationsDataGrid">

                        <DataGrid.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFCCCCCC" Offset="0"/>
                                <GradientStop Color="White" Offset="0.50"/>
                            </LinearGradientBrush>
                        </DataGrid.BorderBrush>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ISBN" Binding="{Binding Publications.Isbn}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}"></DataGridTextColumn>
                            
                            <DataGridTemplateColumn Header="Author(s)" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding AuthorPublications.Author.AuthorName}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Copies">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox 
                                            x:Name="XXX"
                                            SelectedIndex="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ScrollViewer>
            <StackPanel Margin="10" Grid.Column="1">
                <TextBlock>Infos du livre ici</TextBlock>
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
