<Window x:Class="WpfApp.View.Author.CreateAuthorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.View.Author"
        mc:Ignorable="d"
        Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Title="Add author view">
    <Border Style="{StaticResource PrimaryBorder}" Background="#FFEFEFEF" CornerRadius="0,0,20,20">
        <StackPanel>
            <Label Style="{StaticResource LabelHeaderLite}" Content="Add an author"></Label>

            <Label Style="{StaticResource LabelInfo}" Content="Name"></Label>
            <TextBox x:Name="AuthorNameInput" Style="{StaticResource InputText}"/>

            <Label Style="{StaticResource LabelInfo}" Content="Nationality"></Label>
            <TextBox x:Name="AuthorNationalityInput" Style="{StaticResource InputText}"/>

            <Button Style="{StaticResource AddButton}" Content="ADD NEW" Click="CreateAuthorButton_Click"></Button>
            <Button Style="{StaticResource DeleteButton}" Content="BACK" Click="QuitAuthorViewButton_Click"/>

        </StackPanel>
    </Border>
</Window>
